@{
  Layout = "_Layout";
}

@using RecipeBox.Models;

<h1>Welcome to the Recipe Box!</h1>

@* @if (@Model.Count == 0)
  {
  <h3>No recipes have been added yet!</h3>
  }

  <ul>
  @foreach (Recipe recipe in Model)
  {
  <li>@Html.ActionLink($"{recipe.Title}", "Details", new { id = recipe.RecipeId })</li>
  }
  </ul>
} *@

<p>@Html.ActionLink("Add new recipe", "Create", "Recipes")</p>
<p>@Html.ActionLink("Register here", "Register", "Account")</p>
<p>@Html.ActionLink("Show all ratings", "Index", "Ratings")</p>